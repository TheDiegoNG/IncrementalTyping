export class Achievement {
  name: string;
  description: string;

  constructor(achievementName: string, achievementDesc: string) {
    this.name = achievementName;
    this.description = achievementDesc;
  }
}
